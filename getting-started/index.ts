function example(a:number,b:number):number{
  return a+b;
}

example(2,4)

/*typescript 적용할 폴더에서 터미널 열고 
npm i typescript -g 설치.
-> tsc 명령어 사용하기 위해서 설치
tsc index.ts 
-> index.js 파일이 생긴다. 

실제로는 매번 명령어로 tsc index.ts 하는것보다
타입스크립트 웹팩** 사용한다.
*/